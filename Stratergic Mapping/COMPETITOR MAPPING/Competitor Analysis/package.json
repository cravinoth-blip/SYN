{
  "name": "competitor-analysis",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build",
    "lint": "npm --workspace apps/web run lint",
    "api:dev": "uvicorn app.main:app --reload --host 127.0.0.1 --port 8006 --app-dir apps/api",
    "api:test": "pytest apps/api/tests"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
