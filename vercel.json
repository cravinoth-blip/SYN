{
  "buildCommand": "cd ai-platform && npm install && npm run build",
  "outputDirectory": "ai-platform/dist",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
